CREATE SCHEMA IF NOT EXISTS "aws-demo";

CREATE TABLE IF NOT EXISTS "aws-demo"."user"
(
    id BIGINT NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    name VARCHAR NOT NULL,
    email VARCHAR NOT NULL
);
